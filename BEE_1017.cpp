#include <bits/stdc++.h>
using namespace std;

int main()
{
    int A, B;
    double C;
    cin >> A >> B;
    C = (double(A) * B) / 12;
    cout << fixed << setprecision(3) << C << endl;
    return 0;
}