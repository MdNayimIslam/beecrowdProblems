#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N, T;
    float A;
    cin >> N >> T >> A;
    cout << fixed << setprecision(2) << "NUMBER = " << N << endl
         << "SALARY = U$ " << T * A << endl;
    return 0;
}